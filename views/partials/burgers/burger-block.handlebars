<li>
	{{burger}}

	<button class="change-eated_up" data-id="{{id}}" data-newsleep="{{eated_up}}">
		{{#if eated_up}}All Eated Up!{{else}}Eat Me!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
